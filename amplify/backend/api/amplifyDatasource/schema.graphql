type Tasks @model {
  id: ID!
  title: String
  body: String
  status: String
  apartOf: Team @connection(name: "teamTasks")
}

type Team @model {
  id: ID!
  name: String
  tasks: [Tasks] @connection(name: "teamTasks")
}


